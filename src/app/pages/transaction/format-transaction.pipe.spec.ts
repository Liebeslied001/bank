import { FormatTransactionPipe } from './format-transaction.pipe';

describe('FormatTransactionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTransactionPipe();
    expect(pipe).toBeTruthy();
  });
});
